const User = require('../models/user.model');

const createUser = async (userData) => {
    // Implementación del servicio de creación de usuarios (si es necesario)
};

module.exports = { createUser };
